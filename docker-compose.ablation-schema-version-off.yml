services:
  bank-a:
    environment:
      CONTROL_SCHEMA_VERSION_ENABLED: "false"

  bank-b:
    environment:
      CONTROL_SCHEMA_VERSION_ENABLED: "false"

  bank-c:
    environment:
      CONTROL_SCHEMA_VERSION_ENABLED: "false"

  utbetalningsmyndigheten:
    environment:
      CONTROL_SCHEMA_VERSION_ENABLED: "false"
